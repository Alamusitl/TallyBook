<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View"/>

        <variable
            name="guide"
            type="com.owl.tallybook.introduce.GuideModel"/>

        <variable
            name="presenter"
            type="com.owl.tallybook.introduce.GuideFragment.Presenter"/>
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/color_black">

        <android.support.v4.view.ViewPager
            android:id="@+id/id_view_pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:background="@color/color_orange"/>

        <LinearLayout
            android:id="@+id/id_guide_doc_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="@dimen/guide_point_margin_bottom"
            android:orientation="horizontal"
            android:visibility="@{guide.isLastView ? View.INVISIBLE : View.VISIBLE}">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:contentDescription="@string/contentDescription"
                android:padding="@dimen/guide_point_padding"
                android:src="@{guide.isSelected(0) ? @drawable/point_enable : @drawable/point_disenable}"/>

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:contentDescription="@string/contentDescription"
                android:padding="@dimen/guide_point_padding"
                android:src="@{guide.isSelected(1) ? @drawable/point_enable : @drawable/point_disenable}"/>

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:contentDescription="@string/contentDescription"
                android:padding="@dimen/guide_point_padding"
                android:src="@{guide.isSelected(2) ? @drawable/point_enable : @drawable/point_disenable}"/>

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:contentDescription="@string/contentDescription"
                android:padding="@dimen/guide_point_padding"
                android:src="@{guide.isSelected(3) ? @drawable/point_enable : @drawable/point_disenable}"/>

        </LinearLayout>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/id_guide_doc_layout"
            android:layout_alignEnd="@id/id_guide_doc_layout"
            android:layout_alignLeft="@id/id_guide_doc_layout"
            android:layout_alignRight="@id/id_guide_doc_layout"
            android:layout_alignStart="@id/id_guide_doc_layout"
            android:layout_alignTop="@id/id_guide_doc_layout"
            android:background="@drawable/button_shape"
            android:onClick="@{() -> presenter.onEnterMainClick()}"
            android:text="@string/enter_main"
            android:visibility="@{guide.isLastView ? View.VISIBLE : View.GONE}"/>
    </RelativeLayout>

</layout>